# Core FastAPI dependencies (from signvision)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Database - Async PostgreSQL
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0

# AI / LLM
groq>=0.4.0

# Semantic search (already installed in signvision env)
sentence-transformers>=2.2.0

# Fuzzy string matching for product names
rapidfuzz>=3.6.0

# Image handling for invoice uploads
pillow>=10.0.0
